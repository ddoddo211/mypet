<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="petIns">

		<!-- 보험상품 리스트 목록가지고 오는 부분 -->
		<select id="prodPageList" parameterType="pageVo" resultType="pageVo">
			select * from
			(select ROWNUM rnum,
			A.*
			from (select * from insProd order by to_number(insp_id) asc) A)
			WHERE rnum between #{page}*#{pageSize}-(#{pageSize}-1) and #{page}*#{pageSize}
		</select>
		
		<!-- 게시글 총갯수 -->
		<select id="getInsProductCnt" resultType="int">
			select count(*) from insprod
	  	</select>
		
</mapper>